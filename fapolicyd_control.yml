---
- name: Manage fapolicyd Service on RHEL
  hosts: "{{ target_hosts }}"
  become: true

  tasks:
    - name: Set fapolicyd to {{ action }}
      ansible.builtin.service:
        name: fapolicyd
        state: "{{ action }}"
